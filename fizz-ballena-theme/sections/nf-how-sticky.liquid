{%- assign step_count = section.blocks.size | at_least: 1 -%}
<section
  class="nf-how"
  id="{{ section.settings.anchor_id | default: 'how' }}"
  data-nf-how
  style="--how-steps: {{ step_count }};"
>
  <div class="nf-how__track" data-nf-how-track>
    <div class="nf-how__sticky">
      <div class="nf-how__stage">
        {%- for block in section.blocks -%}
          <figure
            class="nf-how__layer{% if forloop.first %} is-active{% endif %}"
            data-nf-how-layer
            {{ block.shopify_attributes }}
          >
            {%- render 'nf-media',
              video: block.settings.video,
              image: block.settings.image,
              asset: block.settings.asset,
              alt: block.settings.title,
              class: 'nf-how__img',
              sizes: '(min-width: 750px) 55vw, 100vw'
            -%}
          </figure>
        {%- endfor -%}
        <div class="nf-how__stage-meta">
          <span class="nf-how__counter" data-nf-how-counter>01 / {{ step_count | prepend: '0' | slice: -2, 2 }}</span>
          <span class="nf-how__progress" aria-hidden="true"><span class="nf-how__progress-fill"></span></span>
        </div>
      </div>

      <div class="nf-how__panel">
        <div class="nf-how__head">
          {%- if section.settings.eyebrow != blank -%}
            <p class="nf-eyebrow">{{ section.settings.eyebrow | escape }}</p>
          {%- endif -%}
          {%- if section.settings.heading != blank -%}
            <h2 class="nf-heading nf-how__heading">{{ section.settings.heading | escape }}</h2>
          {%- endif -%}
        </div>
        <ol class="nf-how__steps">
          {%- for block in section.blocks -%}
            <li
              class="nf-how__step{% if forloop.first %} is-active{% endif %}"
              data-nf-how-step
              {{ block.shopify_attributes }}
            >
              <button
                type="button"
                class="nf-how__step-trigger"
                data-nf-how-goto
                aria-current="{% if forloop.first %}true{% else %}false{% endif %}"
                aria-label="Go to step {{ forloop.index }}: {{ block.settings.title | escape }}"
              >
                <span class="nf-how__step-num" aria-hidden="true">{{ forloop.index | prepend: '0' | slice: -2, 2 }}</span>
                <span class="nf-how__step-title">{{ block.settings.title | escape }}</span>
              </button>
              {%- if block.settings.body != blank -%}
                <div class="nf-how__step-body">{{ block.settings.body }}</div>
              {%- endif -%}
              {%- if block.settings.button_label != blank and block.settings.button_link != blank -%}
                {% render 'nf-button',
                  label: block.settings.button_label,
                  url: block.settings.button_link,
                  style: block.settings.button_style,
                  class: 'nf-btn--pill nf-how__step-cta'
                %}
              {%- endif -%}
            </li>
          {%- endfor -%}
        </ol>
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "NF How To Use",
  "tag": "section",
  "class": "nf-how-section",
  "limit": 1,
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "How it works"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Fizz in three moves"
    },
    {
      "type": "text",
      "id": "anchor_id",
      "label": "Anchor ID",
      "default": "how"
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Fill with cold water"
        },
        {
          "type": "richtext",
          "id": "body",
          "label": "Body",
          "default": "<p>Cold water carbonates best. Fill to the line, no further.</p>"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Photo"
        },
        {
          "type": "video",
          "id": "video",
          "label": "Video",
          "info": "Overrides the photo. Autoplays muted."
        },
        {
          "type": "text",
          "id": "asset",
          "label": "Theme asset fallback",
          "info": "Advanced: filename of a theme asset."
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button style",
          "options": [
            { "value": "fill", "label": "Fill" },
            { "value": "outline", "label": "Outline" },
            { "value": "link", "label": "Linked text underline" }
          ],
          "default": "fill"
        }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "NF How To Use",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "title": "Fill with cold water",
            "body": "<p>Cold water carbonates best. Fill to the line, no further.</p>",
            "asset": "nf-lifestyle-how-01.jpg",
            "button_label": "Shop bottles"
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Twist the pod",
            "body": "<p>Lock a CO2 pod into the base and give it a quarter turn.</p>",
            "asset": "nf-lifestyle-how-02.jpg"
          }
        },
        {
          "type": "step",
          "settings": {
            "title": "Sip the sparkle",
            "body": "<p>Add a flavor pack if you like. Bubbles in under ten seconds.</p>",
            "asset": "nf-lifestyle-how-03.jpg",
            "button_label": "Shop flavors"
          }
        }
      ]
    }
  ],
  "enabled_on": {
    "templates": ["index", "page", "product"]
  }
}
{% endschema %}
