# July 14 Custom Design

**Live package:** [`fizz-july-14th-theme/`](../../fizz-july-14th-theme/) — push with:

```sh
shopify theme push --path fizz-july-14th-theme --store g9rykd-jt.myshopify.com --live --allow-live
```

The `july14-*` files under `fizz/` are an alternate-template experiment only. Do **not** push `fizz/` as the July 14 theme (its default `index.json` is the older Stone Sip / dark hero).

Active theme ID (FIZZ intro): `188630794525`  
Mistaken dark draft from earlier `fizz/` push: `188656681245` (safe to rename/delete in Admin).
